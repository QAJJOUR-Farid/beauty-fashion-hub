import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
      {
        protocol: 'http',
        hostname: '**',
      },
    ],
  },
  // Suppress hydration warnings in production as well
  experimental: {
    // any experimental features if needed
  }
};

export default nextConfig;
